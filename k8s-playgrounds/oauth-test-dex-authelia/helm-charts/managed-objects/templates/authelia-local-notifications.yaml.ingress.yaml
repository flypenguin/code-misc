---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authelia-local-notifications
  namespace: authelia-local
  labels:
    {{- include "managed-objects.labels" . | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: internal-ca
spec:
  tls:
    - hosts:
        - "auth-local-notifications.localhost.traefik.me"
      secretName: tls-auth-local-notifications
  rules:
    - host: auth-local-notifications.localhost.traefik.me
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: authelia-local-notifications
                port:
                  name: filemanager
