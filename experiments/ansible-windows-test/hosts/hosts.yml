all:
  hosts:
    win0: { ansible_host: 192.168.179.150 }

  # for ALL hosts
  vars:

  # groups, etc.
  children:
    win_servers:
      vars:
        ansible_user: "Administrator"
        ansible_password: Administrator1
        ansible_connection: winrm
        ansible_winrm_transport: ntlm
        ansible_winrm_server_cert_validation: ignore
      hosts:
        # that's the host
        win0:
        ad0:
        ad1:
        orbis_1:
        orbis_2:
        fs0:
        fs1:

    kis_servers:
      hosts:
        # these are the hosts
        win0:
        linux0:
        orbis_pacs:

    linux_servers:
      hosts:
        # die hosts wieder
        orbis_pacs:
